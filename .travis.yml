sudo: required
language: node_js

node_js:

  - "0.10"

before_script:
  - npm install -g grunt-cli

deploy:
  provider: openshift
  user: "merloalejandro@gmail.com"
  password: "f19c59x" # can be encrypted
  domain: "amerlo"
  app: nodejs